A lightweight image generator web app built with Next.js 16 (App Router) and OpenAI. Generate images from text prompts with a clean UI and minimal configuration — ideal for demos, prototypes, and learning how to integrate OpenAI's image API into a modern Next.js app. ✨🤍

Features
Built with Next.js 16 using the App Router
Image generation via OpenAI's Images API (DALL·E-style)
Simple, responsive UI for prompt input and result preview
Server-side API route to securely call OpenAI (keeps your API key out of the client)
Error handling and basic usage limits (configurable)
Ready to deploy (Vercel, Netlify, or any Node-compatible host)
